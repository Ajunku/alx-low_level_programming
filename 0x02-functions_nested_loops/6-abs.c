#include "main.h"

/**
 * -abs - Computes the absolute value
 *  of an integer.
 *
 *  @i: input number as an integer.
 *
 *  Return: absolute value
 */
int -abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
